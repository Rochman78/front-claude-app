ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
FRONT_API_TOKEN=your-frontapp-token-here

# Auto-draft des devis (cron Render polling /api/plugin/auto-draft-poll)
AUTO_DRAFT_SECRET=long-random-string

# AUTO_SEND_ENABLED=true → l'auto-draft des devis ENVOIE le mail direct au client
# (au lieu de juste poser un brouillon).
# Si false ou absent (défaut), comportement historique : on pose juste un brouillon
# que l'équipe relit avant envoi. Toggle = kill switch en cas de souci, pas besoin
# de revert de code.
AUTO_SEND_ENABLED=false

# (Optionnel) Token Front dédié à l'envoi auto, séparé du token principal.
# Doit avoir le scope `messages:send` en plus des scopes habituels
# (conversations:read, messages:read, inboxes:read, teammates:read).
# Si défini, send-message utilise ce token. Sinon fallback sur FRONT_API_TOKEN.
# Utile pour isoler le scope sensible "envoi" et pouvoir le révoquer
# sans toucher au token principal qui fait tourner le reste de l'app.
FRONT_API_TOKEN_SEND=your-send-only-token-here
